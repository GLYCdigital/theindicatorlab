User-agent: *
Allow: /
Sitemap: {{ .Site.BaseURL }}sitemap.xml

# Block affiliate redirect pages from indexing
Disallow: /go/

# Block paginated thin pages
Disallow: */page/

# Block RSS/Atom feeds from search results (sitemap.xml stays accessible)
Disallow: /index.xml

# Block search pages (thin content)
Disallow: /search/
